# Word-Vectors
Word vector analysis project implementing cosine similarity, Euclidean distance, and a get_country analogy function. Uses pandas for data handling, predicts words via vector relationships, evaluates accuracy, and visualizes embeddings with PCA and matplotlib.
